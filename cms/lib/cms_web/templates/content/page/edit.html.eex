<h1>Edit Page</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.content_page_path(@conn, :update, @page)) %>

<span><%= link "Back", to: Routes.content_page_path(@conn, :index) %></span>
